Config = {}

Config.iscommand = true --if u want to use it with command make it true 'i might be add item usage later'
Config.Command = 'airsusp'
Config.CloseWork = true --if u want to use it while close to vehicle make it true

